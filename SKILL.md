---
name: yt2note
description: 유튜브 링크·LLM 대화 공유링크·로컬 파일에서 내용을 추출해 Haiku로 요약하고 Obsidian 볼트에 한국어 지식노트로 저장한다. 옵시디언·지식노트 요청 시 우선 사용. 사용법 - /yt2note <유튜브·대화 링크 또는 파일 경로 1개 이상>
---

# yt2note — 소스 → Obsidian 지식노트

인자로 받은 각 소스(유튜브 URL·LLM 대화 공유링크·로컬 파일)를 지식노트로 만들어 볼트에 저장한다.
**핵심 원칙: 요약 모델의 출력을 절대 그대로 믿지 않는다. 저장 전 결정적 검증을 통과해야 하고, 실패하면 저장하지 않는다.**

볼트 경로: `<VAULT>` — **사용자 본인의 Obsidian 볼트 경로로 바꿔 사용한다** (예: `~/Library/Mobile Documents/iCloud~md~obsidian/Documents/<볼트명>`). 아래 절차의 `<볼트>`도 모두 이 경로를 가리킨다.

## 작성 규칙 (전 노트 공통)
- **분량**: 완성된 .md 파일은 **70줄 미만**이어야 한다. 저장 직전 `wc -l`로 확인한다.
- **v2 분할**: 한 노트에 담기 어려울 만큼 내용이 많아 70줄을 넘기면, 두 노트로 나눈다 → `<제목>.md`(1부)와 `<제목> v2.md`(2부). 1부=핵심 요약+주요 내용, 2부=지식 포인트+심화 내용. 두 노트는 서로 `## 관련 노트`로 링크한다. 각 파일 모두 70줄 미만. 문장을 기계적으로 자르지 말고 의미 단위로 나눈다.
- **핵심 키워드 필수**: 모든 노트에 `## 핵심 키워드` 섹션을 넣고 **각 키워드에 한 줄 설명**을 단다(`- **키워드**: 설명`). 사용자가 나중에 제목·키워드만 훑어도 내용이 떠오르게 하는 것이 목적이다(리마인드 용도).

## 절차 (URL마다)

### 1. 내용 추출 (토큰 0) — 소스 유형별 분기
소스 유형을 판별해 아래 중 하나로 본문(`text`)과 `title`·`source`를 확보한다. 어느 경우든 실패하면 그 소스는 건너뛰고 결과표에 사유를 적는다.

- **유튜브 URL** (youtube.com/watch, youtu.be 등):
  ```bash
  python3 ~/.claude/skills/yt2note/scripts/fetch_captions.py <URL>
  ```
  JSON으로 `title`(실제 영상 제목), `channel`, `language`, `text`(자막 전문). 자막 없으면 실패(오디오 받아쓰기 안 함). `source`=영상 URL.
- **LLM 대화 공유링크** (chatgpt.com/share, claude.ai/share, gemini 공유 등): 브라우저로 열어(`preview_start {url}`) 대화 전문을 읽는다. 가상 스크롤로 `get_page_text`가 일부만 잡히면, 페이지 내장 스크립트/데이터(`__reactRouterContext`, `__NEXT_DATA__`, `"mapping"` 등)에서 전체 대화를 추출한다. `title`=대화 제목, `channel`=LLM 이름(ChatGPT 등), `source`=공유 URL.
- **로컬 파일** (.md/.txt/.pdf 등 경로): Read 도구로 내용을 읽는다. `title`=문서 제목(없으면 파일명), `channel`="파일", `source`=파일 경로.

### 2. 중복 확인 (토큰 0)
볼트 전체에서 **소스 식별자**(유튜브 `video_id` / 대화 공유링크 / 파일 경로)를 grep. 이미 있으면 건너뛰고 표에 "중복"이라 적는다.

### 2.5 사전 스캔 — 겹칠 만한 기존 노트 미리 찾기 (토큰 0)
요약 **전에** 볼트의 기존 노트 제목 목록을 만든다(4.5의 find와 동일). 영상 제목·채널·자막의 핵심 명사를 키워드로, 제목이 **같은 대상·개념·시리즈**를 다루는 노트를 0~5개 후보로 추린다.
- 이 후보를 3단계 요약 에이전트에 **컨텍스트로 넘긴다**: "다음 기존 노트와 겹칠 수 있으니, 겹치면 note_title을 그 계열과 일관되게 짓고 `## 관련 노트`·병합 후보로 고려하라."
- **배치 상호 인지(필수)**: 한 번에 여러 URL을 병렬 처리하면 같은 배치에서 동시에 만들어지는 노트끼리는 아직 볼트에 없어 서로 못 본다(형제 노트 연결 누락의 주원인). 이번 배치의 다른 영상 제목·주제도 후보에 포함시켜, 같은 시리즈로 묶이는 **형제 노트끼리 서로 `## 관련 노트`로 연결**되게 한다.
- 후보가 없으면 그대로 진행한다(억지 매칭 금지). 최종 링크·병합 판정은 요약 뒤 4.5·4.6에서 확정한다.

### 2.6 자막 사전 정제 + 조기 컷 (토큰 0 — 결정적 코드)
요약 호출 **전에** 코드로 자막(`text`)을 정제해 입력 토큰을 줄인다:
- 정규식으로 명백한 정형 잡담·군더더기를 먼저 제거한다: 구독·좋아요·알림설정 요청, 광고·협찬 고지, 타임스탬프(`00:00`류), 반복되는 인사/추임새, 완전 중복 줄. (의미 있는 문장은 건드리지 않는다 — 잡담의 최종 판정은 여전히 3단계가 한다.)
- **조기 컷**: 정제 후 본문이 임계치(**500자 미만**)면 3단계 요약을 **호출하지 않고** 바로 `insufficient`(잡담 위주/지식 밀도 부족)로 처리해 저장하지 않는다.
- 3단계에는 원문 대신 **정제된 자막**을 넘긴다.

### 3. Haiku 요약
Agent 도구로 **model: haiku** 서브에이전트를 띄워 요약시킨다. 여러 URL이면 병렬로 띄운다.
프롬프트에 반드시 포함할 것:
- 추출 본문 전문 + 실제 제목 + 채널/소스명
- 2.5의 사전 스캔 후보(있으면): 주제가 겹치면 제목을 그 계열과 일관되게 짓고 관련 노트·병합 후보로 고려
- "추출 본문에 있는 내용만 사용, 외부 지식 보강 금지"
- **잡담 배제**: 사담·인사·자기소개·게스트/코너 소개·광고·구독요청·방청 리액션·진행 멘트 등 지식과 무관한 부분은 요약에서 완전히 뺀다. 정보·개념·사실·주장·설명·데이터만 추출한다.
- **대화형 소스(LLM 대화링크)**: 사용자 발언의 견해·주장·해석도 지식으로 취급한다. 지식성 사용자 의견이 있으면 `knowledge_points`와 별개로 `user_views`(불릿 2~5)로 반환하고, 저장 시 `## 사용자 관점` 섹션으로 넣는다(없으면 생략).
- **억지 채움 금지**: 잡담을 걷어낸 뒤 **남는 실제 지식만** 쓴다. 내용이 빈약해도 일반론·교과서 지식으로 채우지 않는다(이게 예전 '호감 형성' 노트가 저접지로 걸린 원인). 남는 지식이 너무 적으면 요약을 만들지 말고 `{"insufficient": true, "reason": "잡담 위주 / 지식 밀도 부족"}`을 반환한다.
- 섹터는 **가급적** 기존 폴더 중 하나에 넣는다: 경제·금융, 공학·산업·우주, 과학, 국제정세·지정학, 사회·정치, 역사·문화, 예술·콘텐츠, 의학·건강·심리, 철학·인문·문화, 컴퓨터·AI (`개인`은 수동 개인 폴더이므로 요약노트 대상 아님). 위 어디에도 자연스럽게 맞지 않을 때만 **새 섹터를 하나 만든다**(짧은 명사구, 기존과 중복·유사 금지). 새 섹터명은 5단계에서 폴더로 자동 생성된다.
- 반환 형식: JSON `{note_title, sector, tags(3~5개 짧은 검색용 키워드), summary(2~3문장 문단), main_points(불릿 4~6), knowledge_points(불릿 2~4), keywords(3~6개, 각 {term, desc} — term=핵심 개념어, desc=한 줄 설명), user_views(대화형 소스에서 사용자 의견이 있을 때만, 불릿 2~5)}`
- note_title은 **제목만 읽어도 내부 내용을 예상할 수 있게** 구체적으로 짓는다. 다루는 핵심 대상·개념·결론을 제목에 담고, 모호한 일반명(예: "AI 이야기", "우주 탐사")·자극적 낚시성 표현은 금지. 자극적 제목의 영상이면 중립적·서술적으로 재서술한다.
- **제목 형식(직관·변별 우선, 26-07-11 추가)**: 핵심 대상·개념을 **맨 앞에** 둔다(폴더가 이미 분류이므로 섹터·수식어 반복 금지). "~로 본 / ~의 의미 / ~가 바꾼…" 같은 서술 꼬리를 잘라 **명사구**로 짓는다. 길이는 **20자 안팎을 기준으로 하되 하드컷이 아님** — 두 주제를 담으면 조금 길어져도 된다. 같은 폴더 안 유사 노트끼리는 **초점이 구분되게**(변별성) 짓는다.
- 완성 노트가 70줄을 넘지 않도록 간결하게. 내용이 너무 많으면 응답에 그 사실을 알리고 1부/2부로 나눌 지점을 제안한다.

### 4. 접지 검증 (토큰 0 — 결정적 코드)
저장 전 반드시 확인. 하나라도 실패하면 **저장하지 않고** 표에 실패로 기록:
- 요약이 `insufficient`면 저장하지 않고 "잡담 위주/지식 밀도 부족"으로 기록.
- note_title과 summary에서 핵심 명사를 뽑아 추출 본문(text)에 실제로 등장하는지 확인.
  핵심 명사 과반이 본문에 없으면 환각 또는 잡담 위주로 판정하고 저장 거부(접지율도 함께 기록).
- sector가 기존 폴더명과 일치하거나, 불일치 시 **짧은 명사구(신규 섹터)로 유효한지** 확인한다(긴 문장·환각성 문자열 거부). 기존 폴더와 철자·순서만 다른 near-duplicate(예: `사회·정치`↔`정치·사회`)는 기존명으로 교정한다.

### 4.5. 관련 노트 연결 (그래프 연결 — 필수)
저장 전, 볼트의 **기존 노트 제목 목록**을 만든다:
```bash
find "<볼트>" -maxdepth 2 -name "*.md" | sed "s#<볼트>/##; s#\.md\$##"
```
> ⚠️ 참고(26-07-10 실측): Bash가 세션 도중부터 **디렉터리 나열(ls/glob/find)을 차단**할 수 있다(홈·볼트 폴더 대상, dangerouslyDisableSandbox도 무효). 위 find가 빈 결과면 이 상태다. 그때 대처: ① **Read/Write/Edit 도구**는 볼트 정상 접근하니 그걸로 읽고 쓴다. ② Bash는 **정확한 파일 경로 read/write만** 가능(이번 세션이 새로 쓴 파일 한정, Obsidian/iCloud 기존 파일은 Bash read 시 PermissionError). 대량 처리는 glob 대신 **알려진 경로 목록**(직전 저장 결과 등)으로 순회할 것. 한글 파일명 **NFC/NFD 정규화** 불일치로 경로 조회가 실패할 수 있으니 주의.
이 목록에서 **새 노트와 주제가 실제로 겹치는 것은 개수 제한 없이 모두** 골라 `## 관련 노트`에 위키링크로 넣는다(3개 상한 폐지, 26-07-11). 억지만 아니면 촘촘히 연결하는 편이 낫다.
- 형식: `- [[<sector>/<파일명 stem>|<노트 제목>]]`
- **컷 기준(필수)**: 단순히 화제가 겹친다고 잇지 않는다. **한쪽의 제목·내용이 다른 쪽 내용을 예측·상기하는 데 실제로 도움이 될 때만** 연결한다(같은 대상·사건·개념·인물을 다루거나 한쪽이 다른 쪽의 배경/후속인 경우).
- **억지 연결 금지**: 단순히 같은 sector라는 이유로 잇지 않는다. 겹치는 게 없으면 이 섹션을 비워도 된다(`- 없음`).
- 링크는 반드시 위 목록에 실재하는 파일명과 정확히 일치해야 한다(그래프에 깨진 링크를 만들지 않는다).
- **역링크(양방향) 필수**: 기존 노트 B에 링크를 걸면, **B 파일의 `## 관련 노트`에도 이 새 노트를 append**한다(4.6의 append 전용 규칙 준용 — B의 기존 본문은 수정하지 않고 해당 줄만 추가, B에 `## 관련 노트`가 없으면 `## 원본 정보` 앞에 새로 만든다). 편도 링크를 남기지 않는다. 같은 배치의 형제 노트끼리도 서로의 `## 관련 노트`에 상호 기재한다.

### 4.6. 유사 노트 병합·분리 (필수 판단)
4.5에서 만든 기존 노트 목록 중, 새 노트와 **주제가 실질적으로 겹치는(같은 대상·개념·사건을 다루는)** 노트가 있으면 별도 파일을 새로 만들지 말고 아래처럼 처리한다. 목적은 "제목만 봐도 내용이 예상되는" 정돈된 볼트를 유지하고 near-duplicate 난립을 막는 것이다.

- **합치기(append 전용)**: 겹치는 기존 노트가 있으면, 새 자막에서 나온 **기존 노트에 없는 새 지식만** 그 기존 파일에 덧붙인다.
  - 기존 본문은 **절대 삭제·수정·덮어쓰지 않는다.** 오직 뒤에 추가만 한다.
  - 추가 블록은 출처가 다름을 표시한다: `## 추가 내용 (<채널>, <오늘 날짜>)` 헤딩 아래에 불릿으로 넣고, 그 아래 `- 출처: <URL>`을 단다. frontmatter의 `source`는 건드리지 않는다.
  - 겹치는 내용까지 그대로 붙여 중복시키지 말 것 — 정말 새로운 포인트만.
- **분리(split)**: 합친 결과 파일이 70줄을 넘으면, 작성 규칙의 v2 분할로 의미 단위로 나눠 `<제목>.md`+`<제목> v2.md`로 저장하고 서로 링크한다. (기존 노트 하나가 이미 두 주제를 담고 있어 쪼개는 게 맞을 때도 같은 방식으로 분리한다.)
- **폴백**: 겹침이 애매하거나 새 노트가 독립 주제로 충분하면 병합하지 말고 5단계대로 **새 파일로 저장하고 4.5의 관련 노트 링크로만 잇는다.** 확신이 없으면 병합보다 별도 저장을 택한다(파괴 위험 회피).
- 병합·분리한 경우 6단계 대조표의 "저장 위치"에 `병합→<대상파일>` 또는 `분리(v2)`로 명시한다.

### 5. 저장 (토큰 0)
파일: `<볼트>/<sector>/<note_title>.md` — 동명 파일이 있으면 `-02` 접미사.
저장 직전 `wc -l`로 **70줄 미만**을 확인한다. 70줄 이상이면 위 "작성 규칙"의 v2 분할로 두 파일(`<제목>.md`, `<제목> v2.md`)로 나눠 각각 70줄 미만으로 저장하고 서로 링크한다.
형식은 기존 코덱스 노트와 통일:

```markdown
---
title: "<note_title>"
source: "<소스 URL 또는 파일 경로>"
sector: "<sector>"
tags: ["...", "..."]
created: <오늘 YYYY-MM-DD>
---

# <note_title>

## 핵심 요약

<summary 문단>

## 주요 내용

- <main_points>

## 사용자 관점
<!-- 대화형 소스에 user_views가 있을 때만. 없으면 이 섹션 전체를 뺀다 -->
- <user_views>

## 지식 포인트

- <knowledge_points>

## 핵심 키워드
- **<term>**: <desc>

## 관련 노트
- <4.5에서 고른 위키링크들. 없으면 "- 없음">

## 원본 정보
- 자료: <소스 URL 또는 파일 경로>
- 출처: <channel / LLM 이름 / "파일">
```

v2(2부) 노트도 같은 형식을 쓰되, `title`은 `<제목> v2`, 본문은 지식 포인트·심화 내용 중심으로 채우고 `## 관련 노트`에 1부를 링크한다.

### 6. 결과 보고 (필수)
모든 소스 처리 후, 사용자에게 대조표를 출력한다. 이 표가 이 스킬의 신뢰 장치다:

| 소스 (유형: 유튜브/대화링크/파일) | 생성된 노트 제목 | 검증 | 저장 위치 |

실패/건너뜀도 사유와 함께 표에 넣는다. 성공했다는 말 대신 대조 증거를 보여준다.
